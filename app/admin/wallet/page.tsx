import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Wallet"
}

const WalletPage = () => {
    return (
        <div>
            WalletPage
        </div>
    )
}

export default WalletPage
