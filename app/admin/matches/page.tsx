import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Matches"
}

const MatchesPage = () => {
    return (
        <div>
            MatchesPage
        </div>
    )
}

export default MatchesPage
