import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Users"
}

const UserPage = () => {
    return (
        <div>
            UserPage
        </div>
    )
}

export default UserPage
