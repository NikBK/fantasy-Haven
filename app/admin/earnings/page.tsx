import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Earnings"
}

const EarningsPage = () => {
    return (
        <div>
            EarningsPage
        </div>
    )
}

export default EarningsPage
